#!/usr/bin/env zsh

cd "${0:h}"

export DEBUG=1
# Public URL can be set to root for debug but needs
# to be set to _actual_ public base URL if reloaded.
export PUBLIC_URL="/"
export PIPELINE_OUTPUT="./_data/output_from_pipeline"

docker-compose down
docker-compose up --build -d
docker-compose logs -f
